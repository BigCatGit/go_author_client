import 'dart:ui';

class Consts {
  static const String title = "后台管理";

  static const primaryColor = Color(0xFF2697FF);
  static const secondaryColor = Color(0xFF2A2D3E);
  static const bgColor = Color(0xFF212332);

  static const defaultPadding = 16.0;

  static const server_host = "http://127.0.0.1:83";
  static const api_login = "/login";
  static const api_logout = "/logout";

  // 测试模式模拟延迟/毫秒
  static const debug_delay = 200;
}
